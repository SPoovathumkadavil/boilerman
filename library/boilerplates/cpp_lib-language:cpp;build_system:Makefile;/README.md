# |||project_name|||

## Build

```sh
make build
```

## Test

```sh
make test
```

## Install

```sh
make install
```

This installs the static library to `$HOME/.local/lib` and the public header to
`$HOME/.local/include`.
