# |||title|||

This template uses standard page margins without a side-note margin.

## Build

```sh
make
```

The template includes local fonts under `fonts/` and imports `template.typ` from
`main.typ`.
